'use client';
import { styled } from '@mui/system';

export const Header = styled('header')``;
export const HeaderPrimaryMain = styled(Header)`
  color: ${(props) => props.theme.palette.primary.main};
`;
