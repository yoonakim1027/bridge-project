'use client';
import { styled } from '@mui/system';

export const LabelPrimaryMain = styled('label')`
  color: ${(props) => props.theme.palette.primary.main};
`;

export const LabelForModule = styled(LabelPrimaryMain)``;
