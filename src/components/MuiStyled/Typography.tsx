'use client';
import { Typography } from '@mui/material';
import { styled } from '@mui/system';

const TypographyMain = styled(Typography)``;

export const TypographySubTitle = styled(TypographyMain)`
  min-height: 50px;
  font-weight: bold;
`;
